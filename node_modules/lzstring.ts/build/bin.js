#! /usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var LZString_1 = require("./LZString");
var path = require("path");
var fs = require("fs");
var dir = path.dirname(process.argv[1]);
var action = process.argv[2];
var parameters = process.argv.splice(3) || [];
if (parameters.length == 0) {
    console.error("\n  Usage: lz-string action <input_file>\n  action:\n    decompressFromBase64\n    compressToUTF16\n    decompressFromUTF16\n    compressToUint8Array\n    decompressFromUint8Array\n    compressToEncodedURIComponent\n    decompressFromEncodedURIComponent\n    compress\n    decompress\n  ");
    process.exit(1);
}
var content = fs.readFileSync(parameters[0], { encoding: 'utf8' });
console.log(LZString_1.LZString[action](content));
